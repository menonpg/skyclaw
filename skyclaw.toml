[skyclaw]
mode = "local"

[gateway]
host = "127.0.0.1"
port = 8080
tls = false

[provider]
name = "anthropic"
api_key = "${ANTHROPIC_API_KEY}"
model = "claude-sonnet-4-6"

[memory]
# Options: sqlite, markdown, soulmate
# Use "soulmate" for RAG + RLM via The Menon Lab's soul.py infrastructure
backend = "soulmate"
path = "soulmate://ray/ray"  # customer_id/soul_id
api_key = "${SOULMATE_API_KEY}"

[memory.search]
vector_weight = 0.7
keyword_weight = 0.3


[agent]
# Keep last 20 exchange pairs in context window (~40 messages)
max_turns = 20
max_context_tokens = 60000
max_tool_rounds = 200

[vault]
backend = "local-chacha20"

[filestore]
backend = "local"

[security]
sandbox = "mandatory"
file_scanning = true
skill_signing = "required"
audit_log = true

[heartbeat]
enabled = false
interval = "30m"
checklist = "HEARTBEAT.md"
# report_to = ""  # Set to your Telegram chat ID for heartbeat reports

[cron]
storage = "sqlite"

[tools]
shell = true
browser = true
file = true
git = true
cron = true
http = true

[observability]
log_level = "info"
otel_enabled = false

[channel.telegram]
enabled = true
token = "${TELEGRAM_BOT_TOKEN}"
# Prahlad (admin) = DMs + groups, Monica & Pi = groups only
allowlist = ["1870707027", "8733394411", "8756119609"]
file_transfer = true
